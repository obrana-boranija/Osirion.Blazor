namespace Osirion.Blazor.Components;

/// <summary>
/// Defines hero section style variants
/// </summary>
public enum HeroVariant
{
    /// <summary>Uses the standard hero presentation.</summary>
    Hero,
    /// <summary>Uses the larger jumbotron presentation.</summary>
    Jumbotron,
    /// <summary>Uses the compact minimal presentation.</summary>
    Minimal
}
