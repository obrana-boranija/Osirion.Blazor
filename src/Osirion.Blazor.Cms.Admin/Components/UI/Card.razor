@namespace Osirion.Blazor.Cms.Admin.Components.UI

<div class="card shadow-sm @CssClass">
    @if (!string.IsNullOrWhiteSpace(Title) || HeaderContent is not null)
    {
        <div class="card-header d-flex justify-content-between align-items-center">
            @if (!string.IsNullOrWhiteSpace(Title))
            {
                <h3 class="card-title mb-0 fs-6 fw-semibold">@Title</h3>
            }
            
            @HeaderContent
        </div>
    }
    
    <div class="card-body @BodyCssClass">
        @ChildContent
    </div>
    
    @if (FooterContent is not null)
    {
        <div class="card-footer">
            @FooterContent
        </div>
    }
</div>