@inherits OsirionComponentBase

<div class="@GetDividerCssClass()" role="separator" @attributes="AdditionalAttributes">
    @if (!string.IsNullOrEmpty(Label))
    {
        <span class="osirion-menu-divider-label">@Label</span>
    }
</div>